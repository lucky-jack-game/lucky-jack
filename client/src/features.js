// 提出版に出す機能の切り替え。**切り替えはここ1箇所だけにする。**
//
// オンライン対戦（とそれにだけ付いている段位・ランクマッチ）とルームは、今回の提出に入れない
// （メニューの情報量と審査を優先）。コードは消していない——
// battle/OnlineMatch.jsx・server/ はそのまま残してあり、ここを true に戻せば段位・ランクの表示
// （ヘッダーの名札・キャリア画面・段位の実績・遊び方の本文）が戻る。
// ロビーの入口（LobbyScreen.jsx の PLAY 1枚）だけは手で戻すこと——入口の数と形は
// 画面の構図そのもので、フラグで出し分ける物ではない。
//
// **段位はオンラインのランクマッチでしか動かない。** オンラインを出さない版で段位を見せると、
// 永遠に STONE のままの表示と、誰も取れない実績が画面に残る。
export const ONLINE_ENABLED = false;
